apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: test
spec:
  memberFrom:
    serviceAccountRef:
      name: test-account
  # TEST ON BELOW LINE
  role: 
  resourceRef:
    kind: PubSubTopic
    name: test-topic